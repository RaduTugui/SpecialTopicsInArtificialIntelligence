# ── Model weights (large binary files — download from Google Drive) ──
*.pth
*.pt
*.ckpt

# ── Virtual environment ──
.venv/
venv/
env/

# ── Python cache ──
__pycache__/
*.py[cod]
*.pyo

# ── HuggingFace model cache (downloaded automatically on first run) ──
.cache/
~/.cache/huggingface/

# ── PyCharm ──
.idea/

# ── OS files ──
.DS_Store
Thumbs.db

# ── Jupyter / Colab notebooks (optional — remove if you want to track them) ──
*.ipynb_checkpoints/

# ── Dataset (too large for git — keep on Google Drive) ──
*.csv
images/
memotion_dataset_7k/